services:
  frontend:
    image: ${FRONTEND_IMAGE}
    ports:
      - "8088:3001"
    env_file:
      - .env
    restart: always
  backend:
    image: ${BACKEND_IMAGE}
    ports:
      - "5055:5173"
    env_file:
      - .env
    restart: always
